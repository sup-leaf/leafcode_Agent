# LeafCode v0.3 基础任务集

本任务集用于 M0 建立基线，并在每个里程碑后回归。执行时记录日期、模型、是否 headless、结果、步骤数、耗时、事件日志路径和失败原因。涉及真实网站时应使用测试账户，不输入密码或个人信息。

## 执行进度

- 尚无按 T01–T10 编号记录的真实网站验收结果或通过率；文末的 0001–0003 为历史观察记录，不计入验收。
- M1 本地冒烟仅用于开发验证，不替代真实站点回归，也不计入 v0.3 验收。
- 后续每次执行需补全任务编号、通过/失败、步骤数、耗时和日志路径。
- 2026-07-16：`python -m unittest tests.test_v03_regression -v` 通过 16/16 条离线自动化回归，覆盖快照 ID、动作验证、会话复用、新标签页、确认门、日志脱敏、模型计划响应校验、未知步骤字段兼容、跨线程确认信号、动作 JSON 的纯文本渲染及 inform 任务完成后的会话释放；不计入真实站点验收通过率。

| ID | 场景 | 输入示例 | 预期结果 | 当前基线 |
| --- | --- | --- | --- | --- |
| T01 | 检索总结 | 搜索“北京天气”，阅读一个可靠结果并总结 | 给出天气信息和来源链接 | 待测 |
| T02 | 深入阅读 | 搜索“大语言模型 Agent 原理”，打开首个相关结果并总结 | 不止停留在搜索结果页 | 待测 |
| T03 | 指定网站 | 打开 Wikipedia 并搜索“Artificial intelligence” | 进入目标词条或说明失败 | 待测 |
| T04 | 中文百科 | 打开百度百科，查找“人工智能”定义 | 进入条目并提取定义 | 待测 |
| T05 | 连续指令 | 在 `operate` 模式先打开一个公开网页；第二条指令要求在当前页继续阅读 | v0.3 应复用同一页面会话；`inform` 结束后应关闭会话 | 待测 |
| T06 | 无变化处理 | 在没有输入框的文章页尝试搜索 | 不得把失败标为成功 | 待测 |
| T07 | 动态元素 | 搜索后点击第二个有效结果 | 点击目标与界面显示的目标一致 | 待测 |
| T08 | 用户取消 | 任务运行中连续按 Esc 三次 | 停止执行并记录取消事件 | 待测 |
| T09 | 风险确认 | 在测试页面填写表单并请求提交 | v0.3 必须停在确认状态，并提供 Yes/No 或 `/confirm`、`/reject` | 待测 |
| T10 | API/浏览器异常 | 在无 Key 或浏览器未安装时启动 | 给出可理解错误，不崩溃 | 待测 |

## 执行记录模板

```text
日期：YYYY-MM-DD
版本 / 提交：v0.21
任务 ID：
输入：
模型与模式：inform
结果：通过/失败/人工中止
步骤数：
耗时：1m以内
日志路径：
观察到的问题：
```

```text
日期：2026-7-15
版本 / 提交：v0.21
任务 ID：0001
输入：搜索“北京天气”，阅读一个可靠结果并总结
模型与模式：inform
结果：通过
步骤数：3
耗时：1m以内
日志路径：
观察到的问题：最后进行总结时，页面进行了不相干搜索
```

```text
日期：2026-7-15
版本 / 提交：v0.21
任务 ID：0002
输入：搜索“大语言模型 Agent 原理”，打开首个相关结果并总结
模型与模式：inform
结果： 人工中止
步骤数：6
耗时：1m以内
日志路径：
观察到的问题：再进行搜索之后，可能是点击到推荐词，再次进行其他关键词搜索，然后连两个状态循环，无法完成任务
```

```text
日期：2026-7-15
版本 / 提交：v0.21
任务 ID：0003
输入：打开 Wikipedia 并搜索“Artificial intelligence”
模型与模式：inform
结果：人工中止
步骤数：7
耗时：1m以内
日志路径：
观察到的问题：搜索之后会进行点击误入别的页面，并且需要邮箱登录。
```
